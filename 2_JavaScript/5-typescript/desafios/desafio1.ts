// Como podemos rodar isso em um arquivo .ts sem causar erros? 

const employee: { code: number, name: string } = {
    code: 10,
    name: "John"
}

// or

interface employee {
    code: number
    name: string
}